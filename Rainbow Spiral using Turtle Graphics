import turtle
import colorsys

t = turtle.Turtle()
t.speed(0)
turtle.bgcolor("black")
hue = 0

for i in range(360):
    col = colorsys.hsv_to_rgb(hue, 1, 1)
    t.pencolor(col)
    t.forward(i * 2)
    t.left(59)
    hue += 0.005

turtle.done()
